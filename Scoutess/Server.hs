-- | This is the main entry point into the server
module Scoutess.Server where

import Scoutess.Config (ScoutessConfig)

-- | start the server running
scoutess :: ScoutessConfig -- ^ configuration
         -> IO ()
scoutess = 
    undefined
